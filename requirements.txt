# ============================================================
# Yurika — Python Dependencies
# ============================================================

# ── Database ─────────────────────────────────────────────────
asyncpg
pgvector
python-dotenv

# ── Web Scraping & Parsing ───────────────────────────────────
beautifulsoup4
lxml
requests
markitdown

# ── Document Chunking ────────────────────────────────────────
unstructured

# ── AI / LLM & Embeddings ────────────────────────────────────
# google-genai handles both the LLM (Gemini) and embeddings (gemini-embedding-2)
# No local models required — no torch, no FlagEmbedding, no sentence-transformers
google-genai

# ── API & Web Server ─────────────────────────────────────────
fastapi
uvicorn
pydantic-settings

# ── Auth ─────────────────────────────────────────────────────
bcrypt
python-jose[cryptography]
google-auth
email-validator