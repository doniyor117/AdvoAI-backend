# ============================================================
# Yurika — Python Dependencies
# ============================================================

# ── Database ─────────────────────────────────────────────────
asyncpg
pgvector
python-dotenv

# ── Web Scraping & Parsing & Email ───────────────────────────
beautifulsoup4
lxml
requests
markitdown[all]
resend
# PINNED-IN: currently resolves only transitively through google-genai. Declared
# directly because app.services.web_search imports it, and an unrelated pin bump
# in google-genai's own deps could otherwise take it away at import time.
httpx

# ── Document Chunking ────────────────────────────────────────
unstructured

# ── AI / LLM & Embeddings ────────────────────────────────────
# google-genai handles both the LLM (Gemini) and embeddings (gemini-embedding-2)
# No local models required — no torch, no FlagEmbedding, no sentence-transformers
google-genai

# ── API & Web Server ─────────────────────────────────────────
# PINNED: unpinned versions let Hugging Face rebuilds drift to a Starlette
# whose CORS preflight omits Access-Control-Allow-Credentials, which breaks
# all credentialed POST requests (login, chat) from the browser. Do not unpin.
fastapi==0.136.3
starlette==1.1.0
uvicorn==0.48.0
pydantic-settings
python-multipart

# ── Auth ─────────────────────────────────────────────────────
bcrypt
python-jose[cryptography]
google-auth
email-validator

# ── Storage ──────────────────────────────────────────────────
aioboto3
# ── Document Generation ──────────────────────────────────────
python-docx
# Renders contracts to PDF from HTML/CSS in-process (no Chromium, no sidecar).
# Needs system libs installed in the Dockerfile: libpango, libpangocairo, libcairo,
# libgdk-pixbuf.
weasyprint
